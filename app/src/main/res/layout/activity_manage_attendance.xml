<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Enter student UID (or later you can search by email) -->
        <EditText
            android:id="@+id/etStudentUid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Student UID"
            android:singleLine="true" />

        <!-- Date (yyyy-MM-dd) -->
        <EditText
            android:id="@+id/etDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Date (yyyy-MM-dd)"
            android:singleLine="true" />

        <!-- Hard-coded course for now -->
        <EditText
            android:id="@+id/etCourseId"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="GENERAL"
            android:hint="Course ID"
            android:singleLine="true" />

        <!-- Buttons to mark attendance -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">

            <Button
                android:id="@+id/btnMarkPresent"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Mark PRESENT" />

            <Button
                android:id="@+id/btnMarkAbsent"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Mark ABSENT"
                android:layout_marginStart="8dp" />
        </LinearLayout>

        <!-- Load attendance list -->
        <Button
            android:id="@+id/btnLoadAttendance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Load Attendance"
            android:layout_marginTop="8dp" />

        <!-- Summary text -->
        <TextView
            android:id="@+id/tvSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Summary: -"
            android:layout_marginTop="8dp" />

        <!-- Simple text area for list (you can later use RecyclerView) -->
        <TextView
            android:id="@+id/tvAttendanceList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Attendance list will appear here"
            android:layout_marginTop="8dp" />

    </LinearLayout>
</ScrollView>
